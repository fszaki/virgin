{
  "name": "virgin",
  "version": "1.0.0",
  "description": "Web server for creating websites",
  "main": "server.js",
  "scripts": {
    "start": "echo 'app/ ist veraltet. Bitte nutzen: docker compose up -d' && exit 1",
    "dev": "echo 'app/ ist veraltet. Bitte nutzen: docker compose up -d' && exit 1",
    "server:start": "scripts/server/start-server.sh",
    "server:kill": "scripts/server/kill-server.sh",
    "server:restart": "scripts/server/restart-servers.sh",
    "server:logs": "docker compose logs -f"
  },
  "keywords": [
    "web",
    "server",
    "express"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1"
  }
}
