# Frontend (veraltet)

Dieses Verzeichnis wurde durch die einheitliche Struktur unter `web/` ersetzt.

- Aktuelle statische Dateien: `web/public`
- Aktuelle Views/Seiten: `web/views`
- Ehemalige Inhalte wurden in `web/legacy/frontend_public` und `web/legacy/frontend_views` archiviert.

Bitte neue Inhalte ausschließlich unter `web/` pflegen.
